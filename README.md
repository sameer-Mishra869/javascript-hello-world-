🔹 What this code explains:
Comments in JavaScript
Shows how to use single-line (//) and multi-line (/* */) comments.
String immutability
The program tries to modify a character inside a string, which does not work because strings in JavaScript are immutable.
String methods
Demonstrates the use of the toUpperCase() method to convert text into uppercase.
Printing output
Uses console.log() to display values in the console.
String concatenation
Combines multiple strings using the + operator to form a greeting message.
Template literal example
Includes a template literal syntax example (shown as a learning case).
Null value usage
Shows how null is used to represent an intentionally empty value.
🔹 Why this code is useful:
This program is useful for beginners learning JavaScript, as it covers common mistakes, basic string operations, and variable handling in a clear and practical way.
